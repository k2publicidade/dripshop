import Link from 'next/link';
import { createClient } from '@/lib/supabase/server';
import { ArrowUpRight } from 'lucide-react';
export default async function Collections(){const db=await createClient();const {data,error}=await db.from('collections').select('*').eq('is_active',true).order('name');if(error)throw error;return <div className="container py-14"><p className="eyebrow">Explore seus universos</p><h1 className="text-5xl mb-10">Nossas coleções.</h1><div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-5">{data?.map((c,i)=><Link key={c.id} href={'/colecao/'+c.slug} className="border bg-[#f1f1f1] p-8 min-h-52 flex flex-col justify-between group"><span className="text-xs text-gray-500">{String(i+1).padStart(2,'0')} / DRIPSHOP</span><div><h2 className="text-2xl flex justify-between gap-4">{c.name}<ArrowUpRight size={22}/></h2>{c.description&&<p className="text-sm text-gray-500 mt-3">{c.description}</p>}</div></Link>)}</div></div>;}
