'use client';
import {useState} from 'react';
import {createClient} from '@/lib/supabase/client';
import Link from 'next/link';
export default function Reset(){const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [status,setStatus]=useState('');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();if(password!==confirm){setStatus('As senhas não coincidem.');return;}setBusy(true);try{const {error}=await createClient().auth.updateUser({password});setStatus(error?'O link expirou ou é inválido. Solicite outro link.':'Senha atualizada. Você já pode acessar sua conta.');}catch{setStatus('Falha de conexão.');}finally{setBusy(false);}}return <div className="container max-w-lg py-20"><h1 className="text-3xl mb-8">Escolha sua nova senha</h1><form onSubmit={submit} className="space-y-5"><label className="admin-field">Nova senha<input required type="password" minLength={8} autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)}/></label><label className="admin-field">Confirme a senha<input required type="password" minLength={8} autoComplete="new-password" value={confirm} onChange={e=>setConfirm(e.target.value)}/></label><button className="btn-primary" disabled={busy}>{busy?'Salvando...':'Atualizar senha'}</button>{status&&<p role="status">{status}</p>}<Link href="/conta" className="text-link">Acessar minha conta</Link></form></div>;}
