import Link from 'next/link';
import type {ContentSection} from '@/lib/content-schema';
export default function Footer({section}:{section?:ContentSection}){if(!section)return null;const groups=[...new Set(section.items.map(i=>i.reference||'Loja'))];return <footer className="street-footer"><div className="street-container footer-grid"><div className="footer-brand"><Link href="/" aria-label="DripShop início" className="street-logo">DRIPSHOP<small>STREETWEAR | BRASIL</small></Link><p>{section.description}</p><span className="eyebrow">{section.title}</span></div>{groups.map(group=><details key={group} className="footer-group" open><summary>{group}</summary><nav aria-label={group}>{section.items.filter(i=>(i.reference||'Loja')===group).map((item,i)=><Link key={i} href={item.link||'/'}>{item.title}</Link>)}</nav></details>)}<div className="footer-signature"><p>CULTURA SEMPRE VIVA.</p><span/><p>BRASIL É CULTURA.</p></div></div><div className="street-container footer-bottom"><span>© {new Date().getFullYear()} DripShop. Todos os direitos reservados.</span><Link href="/p/privacidade">Privacidade</Link></div></footer>;}
