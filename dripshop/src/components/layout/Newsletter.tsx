'use client';
import {useState} from 'react';
import Link from 'next/link';
export default function Newsletter(){const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;setBusy(true);try{const r=await fetch('/api/newsletter',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:new FormData(form).get('email'),consent:true})});if(!r.ok)throw new Error();setMessage('Pronto! Seu email foi cadastrado.');form.reset();}catch{setMessage('Não foi possível cadastrar. Tente novamente.');}finally{setBusy(false);}}return <form onSubmit={submit}><label htmlFor="newsletter-email" className="text-sm block mb-3">Novos drops, direto no seu email.</label><div className="flex border-b border-white/40"><input id="newsletter-email" name="email" type="email" required placeholder="Seu melhor email" className="py-3 w-full min-w-0 bg-transparent text-sm"/><button disabled={busy} className="text-xs whitespace-nowrap pl-3">{busy?'Enviando...':'Quero receber ↗'}</button></div><label className="text-[11px] text-white/60 flex gap-2 mt-4"><input type="checkbox" required/>Aceito receber novidades e li a <Link href="/p/privacidade" className="underline">política de privacidade</Link>.</label>{message&&<p role="status" className="mt-3 text-sm">{message}</p>}</form>;}
